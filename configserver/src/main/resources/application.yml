spring:
  application:
    name: "configserver"
  config:
    activate:
      on-profile: git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/swarna678/microservices-eazybytes-config.git"
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true

server:
  port: 8071